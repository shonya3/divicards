import { TabBadgeElement } from './wc/stashes/tab-badge';
import { REMOVE_ONLY } from './wc/stashes/tab-badge-group';
TabBadgeElement.define();

const name = `Heist ${REMOVE_ONLY}`;
console.log(name.split(REMOVE_ONLY));
